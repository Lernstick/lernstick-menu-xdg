# Automatically added by dh_systemd_enable
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask live-auto-upgrades.service >/dev/null
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge live-auto-upgrades.service >/dev/null
		deb-systemd-helper unmask live-auto-upgrades.service >/dev/null
	fi
fi
# End automatically added section
